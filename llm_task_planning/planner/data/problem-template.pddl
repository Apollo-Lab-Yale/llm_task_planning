(define (problem NAME_TEMP)
  (:domain virtual-home)
  (:objects
    OBJECT_TEMP
    character_1 - Character
  )
  (:init
    PRED_TEMP
    (standing character_1)
  )
  (:goal (and
    GOAL_TEMP
  ))
)